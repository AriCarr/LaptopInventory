<p id="notice"><%= notice %></p>

<h1><%=pluralize(@holdouts.count, 'user')%> who <%="hasn't".pluralize(@holdouts.count)%> logged a computer yet.</h1>
<h3> <%= mail_to holdout_emails(@holdouts), 'Email them all!' %> </h3>

<ul>
<%= render partial: 'holdout', collection: @holdouts, as: :user%>
</ul>
