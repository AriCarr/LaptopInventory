<h3>Editing <%= @computer.possessive_name %></h3>

<% list_init %>

<%= render partial: "#{@computer.Apple? ? 'mac' : 'pc'}_form", locals: {computer: @computer, url: nil} %>

<%= link_to 'Back', computers_path %>
