<div id = "search_results">

<% @computers = Computer.all.where(history: false) if @computers.nil? %>

<%= render partial: 'root_computer', collection: @computers, as: :computer, spacer_template: 'separator' %>

</div>
